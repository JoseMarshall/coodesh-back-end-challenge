export enum Common {
  MongoId = '_id',
  ImportedT = 'imported_t',
  Status = 'status',
}

export enum TimeStamps {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}

export enum Genders {
  Male = 'male',
  Female = 'female',
}

export enum Status {
  Draft = 'draft',
  Trash = 'trash',
  Published = 'published',
}

export enum UserId {
  Name = 'name',
  Value = 'value',
}

export enum Location {
  Street = 'street',
  City = 'city',
  Country = 'country',
  State = 'state',
  PostCode = 'postcode',
  Coordinates = 'coordinates',
  TimeZone = 'timezone',
}

export enum Street {
  Name = 'name',
  Number = 'number',
}

export enum Name {
  Title = 'title',
  First = 'first',
  Last = 'last',
}

export enum Coordinates {
  Latitude = 'latitude',
  Longitude = 'longitude',
}

export enum TimeZone {
  Offset = 'offset',
  Description = 'description',
}

export enum Dob {
  Date = 'date',
  Age = 'age',
}

export enum Registered {
  Date = 'date',
  Age = 'age',
}

export enum Picture {
  Large = 'large',
  Medium = 'medium',
  Thumbnail = 'thumbnail',
}

export enum Login {
  UUID = 'uuid',
  Username = 'username',
  Password = 'password',
  Salt = 'salt',
  MD5 = 'md5',
  SHA1 = 'sha1',
  SHA256 = 'sha256',
}

export enum ApiKeyUsage {
  Date = 'date',
  Count = 'count',
}
