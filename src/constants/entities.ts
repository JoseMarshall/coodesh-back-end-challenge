export enum User {
  Id = 'id',
  Name = 'name',
  Gender = 'gender',
  Location = 'location',
  Email = 'email',
  Login = 'login',
  Dob = 'dob',
  Registered = 'registered',
  Phone = 'phone',
  Cell = 'cell',
  Picture = 'picture',
  Nat = 'nat',
}

export enum ApiKey {
  MongoId = '_id',
  Apikey = 'apiKey',
  Host = 'host',
  Usage = 'usage',
}

export enum CollectionNames {
  Users = 'users',
  ApiKeys = 'apikeys',
}
